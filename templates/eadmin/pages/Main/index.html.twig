<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{ eadmin_styles() }}
    <title>{{ title }}</title>
</head>
    <body>
        <div class="test">
            {{ render(slots) }}
        </div>
    </body>
    {{ eadmin_scripts() }}
</html>